interface:
  display_name: "Start R9700 Production Stack"
  short_description: "Launch the profile runtime and LiteLLM together"
  default_prompt: "Use $start-r9700-runtime to start the selected production profile with its LiteLLM proxy and wait until both are ready."
