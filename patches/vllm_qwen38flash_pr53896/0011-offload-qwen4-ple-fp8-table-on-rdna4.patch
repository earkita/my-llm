diff --git a/tests/models/qwen4_exp/test_qsa_amd.py b/tests/models/qwen4_exp/test_qsa_amd.py
--- a/tests/models/qwen4_exp/test_qsa_amd.py
+++ b/tests/models/qwen4_exp/test_qsa_amd.py
@@ -91,6 +91,46 @@ def test_ple_fp8_embedding_dequantizes_with_checkpoint_scale() -> None:
     )
 
 
+def test_mxfp4_checkpoint_selects_fp8_ple_embedding() -> None:
+    config = SimpleNamespace(
+        get_name=lambda: "compressed-tensors",
+        quant_format="mxfp4-pack-quantized",
+    )
+
+    method = ple_layer_module._get_ple_embedding_quant_method(
+        config, "model.layers.0.ple.ple_embedding.ngram_embedding"
+    )
+
+    assert isinstance(method, ple_layer_module.Qwen4ExpPLEFp8EmbeddingMethod)
+
+
+def test_ple_cpu_offload_constructs_fp8_weight_on_cpu(
+    monkeypatch: pytest.MonkeyPatch,
+) -> None:
+    from vllm.model_executor import parameter as parameter_module
+
+    monkeypatch.setenv("VLLM_PLE_CPU_OFFLOAD", "1")
+    monkeypatch.setattr(parameter_module, "get_tensor_model_parallel_rank", lambda: 0)
+    monkeypatch.setattr(
+        parameter_module, "get_tensor_model_parallel_world_size", lambda: 1
+    )
+    layer = torch.nn.Module()
+    method = ple_layer_module.Qwen4ExpPLEFp8EmbeddingMethod()
+
+    method.create_weights(
+        layer,
+        input_size_per_partition=4,
+        output_partition_sizes=[8],
+        input_size=4,
+        output_size=8,
+        params_dtype=torch.bfloat16,
+    )
+
+    assert layer.weight.device.type == "cpu"
+    assert layer.weight.dtype == torch.float8_e4m3fn
+    assert layer.weight_scale.dtype == torch.bfloat16
+
+
 def test_ple_norm_uses_amd_grouped_kernel(
     monkeypatch: pytest.MonkeyPatch,
 ) -> None:
diff --git a/vllm/models/qwen4_exp/amd/ple_layer.py b/vllm/models/qwen4_exp/amd/ple_layer.py
--- a/vllm/models/qwen4_exp/amd/ple_layer.py
+++ b/vllm/models/qwen4_exp/amd/ple_layer.py
@@ -1,8 +1,9 @@
 # SPDX-License-Identifier: Apache-2.0
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
-"""GPU-resident Qwen4Exp position-learning enhancement layers."""
+"""Qwen4Exp position-learning enhancement layers."""
 
 import math
+import os
 from collections.abc import Iterable, Sequence
 
 import torch
@@ -31,7 +32,10 @@ from vllm.model_executor.layers.vocab_parallel_embedding import (
     VocabParallelEmbedding,
 )
 from vllm.model_executor.models.utils import AutoWeightsLoader
-from vllm.model_executor.parameter import PerTensorScaleParameter
+from vllm.model_executor.parameter import (
+    ModelWeightParameter,
+    PerTensorScaleParameter,
+)
 from vllm.transformers_utils.configs.qwen4_exp import (
     Qwen4ExpTextConfig,
 )
@@ -45,6 +49,11 @@ _SPLITMIX_M2 = 0x94D049BB133111EB
 _PLE_LAYER_PRIME = 10007
 
 
+def _ple_cpu_offload_enabled() -> bool:
+    value = os.getenv("VLLM_PLE_CPU_OFFLOAD", "0").strip().lower()
+    return value in {"1", "true", "yes", "on"}
+
+
 def _splitmix64(value: int) -> int:
     value = (value + _SPLITMIX_GAMMA) & _MASK64
     value = ((value ^ (value >> 30)) * _SPLITMIX_M1) & _MASK64
@@ -153,9 +162,25 @@ class Qwen4ExpPLEFp8EmbeddingMethod(QuantizeMethodBase):
     ) -> None:
         del input_size, output_size, params_dtype
         weight_loader = extra_weight_attrs.get("weight_loader")
-        weight = create_fp8_weight_parameter(
-            sum(output_partition_sizes), input_size_per_partition, weight_loader
-        )
+        if _ple_cpu_offload_enabled():
+            # This table is roughly 51 GiB before TP sharding.  Construct the
+            # local shard directly on the CPU so the default CUDA device
+            # context cannot transiently allocate it on a 32 GiB GPU.
+            weight = ModelWeightParameter(
+                data=torch.empty(
+                    sum(output_partition_sizes),
+                    input_size_per_partition,
+                    dtype=torch.float8_e4m3fn,
+                    device="cpu",
+                ),
+                input_dim=1,
+                output_dim=0,
+                weight_loader=weight_loader,
+            )
+        else:
+            weight = create_fp8_weight_parameter(
+                sum(output_partition_sizes), input_size_per_partition, weight_loader
+            )
         layer.register_parameter("weight", weight)
 
         weight_scale = create_fp8_scale_parameter(
@@ -177,20 +203,37 @@ class Qwen4ExpPLEFp8EmbeddingMethod(QuantizeMethodBase):
         raise NotImplementedError("PLE FP8 weights only support embedding lookup")
 
     def embedding(self, layer: nn.Module, input_: torch.Tensor) -> torch.Tensor:
+        if layer.weight.device.type == "cpu" and input_.device.type != "cpu":
+            # Only the rows needed by this step cross PCIe.  Return them to the
+            # caller's device before TP masking and all-reduce.
+            return F.embedding(input_.to(device="cpu"), layer.weight).to(
+                device=input_.device
+            )
         return F.embedding(input_, layer.weight)
 
 
 def _get_ple_embedding_quant_method(
     quant_config: QuantizationConfig | None,
     prefix: str,
 ) -> QuantizeMethodBase | None:
     """Select global-scale FP8 only for quantized PLE checkpoint shards."""
 
-    if not isinstance(quant_config, Fp8Config):
-        return None
-    if not quant_config.is_checkpoint_fp8_serialized:
+    if isinstance(quant_config, Fp8Config):
+        if not quant_config.is_checkpoint_fp8_serialized:
+            return None
+    elif not (
+        quant_config is not None
+        and quant_config.get_name() == "compressed-tensors"
+        and getattr(quant_config, "quant_format", None) == "mxfp4-pack-quantized"
+    ):
         return None
 
+    # Qwen4Exp MXFP4 checkpoints keep the checkpoint-split PLE table in FP8
+    # even though the broad compressed-tensors exclusion prevents generic
+    # embedding dispatch from recognizing it.
+    if not isinstance(quant_config, Fp8Config):
+        return Qwen4ExpPLEFp8EmbeddingMethod()
+
     ignored_layers = quant_config.ignored_layers
     if is_layer_skipped(
         prefix,
