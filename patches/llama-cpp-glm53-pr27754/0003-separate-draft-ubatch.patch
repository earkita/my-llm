diff --git a/common/arg.cpp b/common/arg.cpp
index 86f8610..f4a86ca 100644
--- a/common/arg.cpp
+++ b/common/arg.cpp
@@ -4038,6 +4038,16 @@ common_params_context common_params_parser_init(common_params & params, llama_ex
             params.speculative.draft.cpuparams_batch.poll = value;
         }
     ).set_spec().set_examples({LLAMA_EXAMPLE_SPECULATIVE, LLAMA_EXAMPLE_SERVER, LLAMA_EXAMPLE_CLI}));
+    add_opt(common_arg(
+        {"--spec-draft-ubatch-size"}, "N",
+        "physical maximum batch size for the draft context (default: same as target)",
+        [](common_params & params, int value) {
+            if (value <= 0) {
+                throw std::invalid_argument("draft ubatch size must be positive");
+            }
+            params.speculative.draft.n_ubatch = value;
+        }
+    ).set_env("LLAMA_ARG_SPEC_DRAFT_UBATCH"));
     add_opt(common_arg(
         {"--spec-draft-type-k", "-ctkd", "--cache-type-k-draft"}, "TYPE",
         string_format(
diff --git a/common/common.h b/common/common.h
index de49dac..18bf66b 100644
--- a/common/common.h
+++ b/common/common.h
@@ -324,6 +324,7 @@ struct common_params_model {
 struct common_params_speculative_draft {
     int32_t n_max = 3; // maximum number of tokens to draft during speculative decoding
     int32_t n_min = 0; // minimum number of draft tokens to use for speculative decoding
+    int32_t n_ubatch = 0; // physical draft batch size (0 = same as target)
 
     float p_split = 0.1f; // speculative decoding split probability
     float p_min   = 0.0f; // minimum speculative decoding probability (greedy)
diff --git a/common/speculative.cpp b/common/speculative.cpp
index 4eef221..80712fc 100644
--- a/common/speculative.cpp
+++ b/common/speculative.cpp
@@ -2390,6 +2390,10 @@ common_speculative_init_result::common_speculative_init_result(
 
     // the draft context holds as many tokens per sequence as the target context
     cparams.n_ctx = llama_n_ctx(ctx_tgt);
+    if (params.speculative.draft.n_ubatch > 0) {
+        cparams.n_ubatch = std::min<uint32_t>(
+                cparams.n_ubatch, params.speculative.draft.n_ubatch);
+    }
 
     // note: for small models maybe we can set this to the maximum possible draft from all speculative types
     //       the extra memory for small models is likely negligible?
